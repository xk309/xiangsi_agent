# 上海污染过程相似度匹配

使用当前预测窗口寻找污染物变化及气象背景相近的历史窗口，供案例对比。

## 业务术语

**预测批次 forecast batch**：一次起报产生的污染物预测，同一有效日可以属于不同批次。

**匹配窗口 matching window**：用户在同一预测批次中选择的连续1—7个有效日期。

**历史候选 historical candidate**：与匹配窗口等长的连续历史日期，污染物使用真实值。

**污染物九宫格 station grid**：上海54个固定站点对应的九个区域；不同于覆盖更大范围的气象九宫格。

**气象九宫格 meteorology grid**：覆盖119.25—123.75°E、29—33.5°N的九个气象区域。

**日标签 daily label**：某日某格某污染物或气象条件的检索分类，不代表正式城市AQI。

**过程特征 process feature**：所选窗口内的方向序列、最高位置及超检索阈值持续情况。

**图像复核 image review**：对当前和历史四类气象图的结构化比较，评分不代表预测准确率。

**历史残差 historical residual**：同站点、同污染物、同预报日序的历史真实值减历史预测值。

**累积污染案例库 cumulative pollution episode library**：以已有五年历史数据为起点，由数据中台持续送入次日污染物与气象模型预测，待对应实况到齐后逐步形成的历史污染过程集合。案例不会因超过五年而自动移出；数据随运行年限持续累积。案例包含污染物实况、气象背景及可关联的历史预测，用于相似过程检索和历史偏差分析。

**案例闭合 case finalization**：某次历史预测对应的污染物和气象实况到齐并通过完整性检查后，该预测窗口才能成为可用于偏差分析的闭合案例；未闭合数据只能用于当前预测展示，不能计算历史预测偏差。

**分析建议 analysis recommendation**：智能体根据当前预测、历史相似过程和专业知识生成的有证据说明的研判意见，供预报员参考，不直接修改预测结果。

**偏差调整建议 bias adjustment recommendation**：根据可比历史案例中“历史实况减历史预测”的偏差，给出的当前预测调整方向、参考范围和可信度；它是建议，不是自动订正结果。

**业务问题 business question**：预报员围绕污染风险、时空变化、形成原因、历史相似过程、偏差调整或专业知识提出的问题。

**系统意图 system intent**：智能体为了完成用户请求而识别的操作类型，例如创建匹配任务、查询进度、读取候选证据或进行专业知识检索；它不同于用户关心的业务问题分类。

**专业知识检索 professional knowledge retrieval**：从已审核的污染物与气象资料中检索可引用知识，为回答提供依据；业务数值、任务结果和气象图通过工具读取，不作为专业知识文档存入向量库。

**空气质量预报智能大屏 air-quality forecasting workspace**：面向预报员的主要交互界面，展示污染物站点数据、气象网格数据及温度、降水、风、相对湿度等要素，并提供相似过程匹配和智能体入口。

**数据权威源 authoritative data source**：业务方服务器保存污染物与气象历史数据的权威副本；本项目通过数据中台接口定时获取增量数据，并保存完成匹配、追溯和回测所需的数据版本、特征及引用关系。

**预报员反馈 forecaster feedback**：预报员对智能体建议的采用情况、最终调整值和采用原因。反馈用于评测和改进系统，不代表智能体自动修改预测结果。
